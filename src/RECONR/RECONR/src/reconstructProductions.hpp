template< typename Range >
static
void summateProductions( std::ostream& output,
                        std::ostream& error,
                        R2D2& data,
                        const Range& energies ){
}
